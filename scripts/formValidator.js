import {profile, profileTitle, profileSubtitle, placeContainer, placeTitle, placePhoto, buttonEditProfile, buttonAddPlace, 
    popupList, popupEditProfile, popupAddPlace, popupImage, popupImagePhoto, buttonSubmitPlace, nameInput, jobInput, placeInput, 
    srcInput, placeTemplate, placeElementTemplate, initialCards} from '.constants.js';

class FormValidator {
    constructor() {

    }

    enableValidation() {
        //включает валидацию формы
    }
}